import React from "react"; import { Select as AntSelect } from "antd"; import { Controller } from "react-hook-form";  interface Props {   label: string;   name: string;   control: any;   required?: boolean;   errors: Record<string, any>;   options: string[];   placeholder?: string; }  export default function MultiSelect({   label,   name,   control,   required = false,   errors,   options,   placeholder = "Sélectionnez des options", }: Props) {   return (     <div className="mb-4">       <label className="block text-gray-700 font-medium mb-2">{label}</label>       <Controller         name={name}         control={control}         defaultValue={[]}         rules={{ required }}         render={({ field }) => {           const selectedItems: string[] = field.value || [];           const filteredOptions = options.filter((o) => !selectedItems.includes(o));            return (             <AntSelect               {...field}               mode="multiple"               style={{ width: "100%" }}               placeholder={placeholder}               options={filteredOptions.map((item) => ({                 value: item,                 label: item,               }))}               status={errors[name] ? "error" : ""}               value={selectedItems}               onChange={(value) => field.onChange(value)}               className={`w-full rounded-lg transition-all duration-300 ${                 errors[name] ? "border-red-500" : "border-gray-300"               }`}             />           );         }}       />       {errors[name] && <p className="text-red-500 text-sm mt-1">Ce champ est requis.</p>}     </div>   ); } 